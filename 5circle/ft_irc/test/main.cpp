#include <iostream>

/***
 * socket, 소켓을 생성함
 * close, 소켓 통신을 종료함
 * setsockopt, 
 * getsockname, 
 * getprotobyname, 
 * gethostbyname, 
 * getaddrinfo, 
 * freeaddrinfo, 
 * bind, 생성된 소켓을 연결함 (바인딩)
 * connect, 
 * listen, 신호 읽을 때까지 기다림
 * accept, 사용자가 접근할 때 허용함
 * htons, 
 * htonl, 
 * ntohs, 
 * ntohl, 
 * inet_addr, 
 * inet_ntoa, 
 * send, 
 * recv, 
 * signal, 
 * sigaction, 
 * lseek, 
 * fstat, 
 * fcntl, 
 * poll 
 ***/

int main(int argc, char **argv){

}